import { InstanceSettings } from 'n8n-core';
import type { WorkflowActivateMode } from 'n8n-workflow';
import type { PubSubCommandMap } from '../events/maps/pub-sub.event-map';
import { Logger } from '../logging/logger.service';
import { MultiMainSetup } from './orchestration/main/multi-main-setup.ee';
export declare class OrchestrationService {
    private readonly logger;
    readonly instanceSettings: InstanceSettings;
    readonly multiMainSetup: MultiMainSetup;
    constructor(logger: Logger, instanceSettings: InstanceSettings, multiMainSetup: MultiMainSetup);
    private publisher;
    private subscriber;
    protected isInitialized: boolean;
    private isMultiMainSetupLicensed;
    setMultiMainSetupLicensed(newState: boolean): void;
    get isMultiMainSetupEnabled(): boolean;
    get isSingleMainSetup(): boolean;
    sanityCheck(): boolean;
    init(): Promise<void>;
    shutdown(): Promise<void>;
    publish<CommandKey extends keyof PubSubCommandMap>(commandKey: CommandKey, payload?: PubSubCommandMap[CommandKey]): Promise<void>;
    getWorkerStatus(id?: string): Promise<void>;
    shouldAddWebhooks(activationMode: WorkflowActivateMode): boolean;
    shouldAddTriggersAndPollers(): boolean;
}
